{
  "name": "claude-export-viewer",
  "version": "1.0.0",
  "description": "Local, single-file web app for browsing exported Claude.ai conversations",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "screenshot": "playwright install chromium && node scripts/screenshot.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
